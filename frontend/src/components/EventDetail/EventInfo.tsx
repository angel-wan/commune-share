const EventInfo = () => {

    return (
        <div>
            <p>this is the description of the event and to be replace by later fetching from db?</p>
        </div>
    );
};

export default EventInfo;
